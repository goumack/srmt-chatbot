apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: srmt-chroma-pvc
  labels:
    app: srmt-documind
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp2  # Modifier selon votre classe de stockage OpenShift
